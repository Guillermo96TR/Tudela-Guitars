<form method="post" action="{{ path('app_bass_guitar_delete', {'id': bass_guitar.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ bass_guitar.id) }}">
    <button class="btn">Delete</button>
</form>
